# Alpaca API Client

This module provides the interface to the Alpaca trading platform API. The `AlpacaClient` class handles authentication, market data retrieval (current prices, option chains), market status checks, and order submission for put credit spreads. It includes comprehensive error handling, retry logic for transient failures, and detailed logging of all API interactions. The client supports both paper trading and live trading environments.
